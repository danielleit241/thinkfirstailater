# Required in production. Use the public origin without a trailing slash.
NEXT_PUBLIC_APP_URL=https://example.com

# Add when the frontend consumes a public backend API.
NEXT_PUBLIC_API_URL=http://localhost:8080/
